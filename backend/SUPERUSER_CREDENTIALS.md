# 🔐 Учетные данные суперпользователя

## 📋 Данные для входа в админ-панель

```
Логин:    admin
Пароль:   admin123
Email:    admin@example.com
```

## 🚀 Быстрая настройка

### Вариант 1: Автоматическое создание (рекомендуется)

```bash
cd backend
source venv/bin/activate

# Примените миграции (если нужно)
python manage.py migrate

# Создайте суперпользователя
python manage.py create_superuser
```

### Вариант 2: Ручное создание

```bash
cd backend
source venv/bin/activate
python manage.py createsuperuser
```

Затем введите:
- Username: `admin`
- Email: `admin@example.com`
- Password: `admin123`

## 🌐 Доступ к админ-панели

После создания суперпользователя откройте:

**Локально:**
```
http://localhost:8000/admin/
```

**В сети:**
```
http://[ваш-ip-адрес]:8000/admin/
```

## ⚠️ Если возникли проблемы с миграциями

Если миграции не применяются из-за UUID, выполните:

```bash
# Удалите базу данных (все данные будут потеряны!)
rm backend/db.sqlite3

# Создайте новую базу
python manage.py migrate

# Создайте суперпользователя
python manage.py create_superuser
```

## 📝 Примечание

Эти учетные данные используются командой `create_superuser`. 
Если пользователь уже существует, пароль будет обновлен.
