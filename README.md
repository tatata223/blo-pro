# Приложение Заметки

Веб-приложение для создания и управления заметками с использованием Django (бэкенд) и React (фронтенд).

## Особенности

- ✅ Система аутентификации пользователей
- ✅ Создание, редактирование и удаление заметок
- ✅ Rich Text Editor с форматированием
- ✅ Шаблоны заметок с SVG иконками
- ✅ Организация заметок по папкам
- ✅ Теги для категоризации
- ✅ Поиск по заметкам
- ✅ Закрепление заметок
- ✅ Современный дизайн с цветовой палитрой (салатовый зеленый, оранжево-желтый, белый)

## Быстрый старт

### Запуск одной командой

**macOS/Linux:**
```bash
./start.sh
```

**Windows:**
```bash
start.bat
```

Скрипт автоматически:
- Создаст виртуальное окружение (если нужно)
- Установит все зависимости
- Выполнит миграции базы данных
- Инициализирует шаблоны
- Запустит оба сервера (бэкенд и фронтенд)

После запуска:
- 🌐 Бэкенд будет доступен на: http://localhost:8000
- 🌐 Фронтенд будет доступен на: http://localhost:3000

Для остановки нажмите `Ctrl+C` (macOS/Linux) или закройте окна (Windows).

## Ручная установка

Если хотите установить вручную:

### Бэкенд (Django)

1. Перейдите в папку backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip3 install -r requirements.txt
```

4. Выполните миграции:
```bash
python3 manage.py migrate
```

5. Создайте суперпользователя (опционально):
```bash
python3 manage.py createsuperuser
```

6. Инициализируйте шаблоны:
```bash
python3 manage.py init_templates
```

7. Запустите сервер:
```bash
python3 manage.py runserver
```

### Фронтенд (React)

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение:
```bash
npm start
```

## Структура проекта

```
ЗаметкиPARADIZE/
├── backend/          # Django бэкенд
│   ├── notes/       # Приложение заметок
│   └── notes_project/  # Настройки проекта
├── frontend/        # React фронтенд
│   └── src/         # Исходный код
├── start.sh         # Скрипт запуска (macOS/Linux)
├── start.bat        # Скрипт запуска (Windows)
└── README.md        # Этот файл
```

## Использование

1. Запустите приложение командой `./start.sh` или `start.bat`
2. Откройте браузер и перейдите на http://localhost:3000
3. Зарегистрируйтесь или войдите в систему
4. Создайте новую заметку или выберите шаблон внизу страницы
5. Используйте панель инструментов для форматирования текста
6. Организуйте заметки по папкам и тегам
7. Используйте поиск для быстрого доступа к заметкам

## API Endpoints

- `/api/auth/login/` - Вход
- `/api/auth/logout/` - Выход
- `/api/auth/register/` - Регистрация
- `/api/auth/user/` - Текущий пользователь
- `/api/notes/` - CRUD операции с заметками
- `/api/folders/` - CRUD операции с папками
- `/api/tags/` - CRUD операции с тегами
- `/api/templates/` - Список шаблонов

## Технологии

- **Backend**: Django 4.2, Django REST Framework
- **Frontend**: React 18, React Router
- **База данных**: SQLite (по умолчанию)
- **Стили**: CSS с переменными

## Требования

- Python 3.8+
- Node.js 16+
- npm или yarn

## Лицензия

MIT
